package com.example.moneyflow.utils

import com.example.moneyflow.R

enum class BottomNavigationPage(val itemId: Int) {
    Home(R.id.nav_home),
    Create(R.id.nav_create),
    Settings(R.id.nav_settings),
}


